server {
    listen 80;
    server_name localhost;

    # ✅ อนุญาตเฉพาะ /upload/<filename> เท่านั้น
    location /upload/ {
        alias /var/www/data/upload/;
        autoindex off;
        try_files $uri =404;
    }

    # ❌ ถ้าเป็น /upload/ ตรง ๆ → 400
    location = /upload/ {
        return 400;
    }

    # ❌ ถ้าเป็น / หรือ path อื่น ๆ ที่ไม่ใช่ /upload/... → 400 ทั้งหมด
    location / {
        return 400;
    }

    # 🟡 400 Error Page
    error_page 400 /400.html;
    location = /400.html {
        root /usr/share/nginx/html/error_pages;
    }

    # 🔴 404 Error Page
    error_page 404 /404.html;
    location = /404.html {
        root /usr/share/nginx/html/error_pages;
    }

    # ⚠️ Default error page
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}